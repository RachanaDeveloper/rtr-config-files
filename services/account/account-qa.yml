service:
  info: Account Microservice (Quality Assurance)
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5550/db_account
    username: rtrpostgres #get username from vault
    password: pwd
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/rtr-ms
server:
  port: 16801
